{
  "$schema": "https://turborepo.com/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": [".next/**", "!.next/cache/**", "dist/**"],
      "env": ["SANITY_STUDIO_PROJECT_ID", "SANITY_STUDIO_DATASET", "SANITY_TOKEN"]
    },
    "//#format-and-lint": {},
    "//#format-and-lint:fix": {
      "cache": false
    },
    "check-types": {
      "dependsOn": ["^check-types"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "typegen": {
      "cache": true,
      "outputs": ["schema.json", "sanity.types.ts"],
      "env": ["SANITY_STUDIO_DATASET", "SANITY_STUDIO_PROJECT_ID"]
    },
    "@apps/web#typegen:watch": {
      "outputs": ["schema.json", "sanity.types.ts"],
      "cache": false,
      "persistent": true
    },
    "@apps/web#build": {
      "dependsOn": ["@apps/web#typegen"],
      "outputs": ["dist/**"],
      "cache": true,
      "env": ["SANITY_STUDIO_PROJECT_ID", "SANITY_STUDIO_DATASET", "SANITY_TOKEN"]
    },
    "@apps/web#dev": {
      "with": ["@apps/web#typegen:watch"],
      "persistent": true
    }
  }
}
