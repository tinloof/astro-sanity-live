{
  "$schema": "https://turborepo.com/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": [".next/**", "!.next/cache/**", "dist/**"],
      "env": ["PUBLIC_SANITY_PROJECT_ID", "PUBLIC_SANITY_DATASET", "SANITY_API_READ_TOKEN"]
    },
    "//#format-and-lint": {},
    "//#format-and-lint:fix": {
      "cache": false
    },
    "check-types": {
      "dependsOn": ["^check-types"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "typegen": {
      "cache": true,
      "outputs": ["schema.json", "sanity.types.ts"],
      "env": ["PUBLIC_SANITY_PROJECT_ID", "PUBLIC_SANITY_DATASET"]
    },
    "astro-caching-demo#typegen:watch": {
      "outputs": ["schema.json", "sanity.types.ts"],
      "cache": false,
      "persistent": true
    },
    "astro-caching-demo#build": {
      "dependsOn": ["astro-caching-demo#typegen"],
      "outputs": ["dist/**"],
      "cache": true,
      "env": ["PUBLIC_SANITY_PROJECT_ID", "PUBLIC_SANITY_DATASET", "SANITY_API_READ_TOKEN"]
    },
    "astro-caching-demo#dev": {
      "with": ["astro-caching-demo#typegen:watch"],
      "persistent": true
    }
  }
}
